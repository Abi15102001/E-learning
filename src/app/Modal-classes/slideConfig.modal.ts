export class SlideConfig {
    breakPoints:{sm: number, md: number, lg: number, xl: number}={sm:1,md:2,lg:3,xl:4}

    showLRarrow:boolean =true;

    showDots:boolean =false;

    autoPlay:boolean =false;
}
